---
title: "PCA Analysis Report"
output:
  html_document:
    self_contained: false
---

# PCA Analysis Report

This report presents the results of PCA analysis.

## Interactive PCA Plot

```{r pca_plot, echo=FALSE}
library(htmlwidgets)
load("pca_results.RData")  # Load PCA results
plot_ly(data = pca_results, x = ~PC1, y = ~PC2, z = ~PC3, color = ~Conditions, type = "scatter3d", mode = "markers")
